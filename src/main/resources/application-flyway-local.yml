mbti:
  infra:
    flyway:
      url: jdbc:mysql://localhost:13306/mbti?allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
      user: root
      password: root
      locations: classpath:db/migration/entity
      sql-migration-suffixes: sql
      baseline-on-migrate: true
      baseline-version: 0